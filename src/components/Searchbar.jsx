import NavBar from "./Navbar";

export default function Searchbar() {
    return (<div>
        <NavBar/>
        <div>
            sorry this functionality will come at a later date! (if i ever come back to this at all)
        </div>
    </div>) 
}